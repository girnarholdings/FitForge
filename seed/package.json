{
  "name": "@fitforge/seed",
  "version": "0.1.0",
  "private": true,
  "description": "FitForge curated seed data (JSON) + generator that compiles supabase/seed/seed.sql (WS-2).",
  "type": "module",
  "scripts": {
    "seed:check": "tsx generate.ts check",
    "seed:generate": "tsx generate.ts",
    "test": "tsx --test test/validate.test.ts test/stream-json.test.ts test/food-shrink.test.ts",
    "food:import": "node import-usda.mjs",
    "food:import:fixture": "node import-usda.mjs --fixture fixtures/fdc-sample.json"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
